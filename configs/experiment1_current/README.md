# Experiment 1: Current Five-Asset Environment

Experiment 1 remains the existing 5-risky-asset plus cash setup. Its market,
allocation constraints, model settings, and training schedules are unchanged.

Use the existing configurations:

- `../simplex_ppo_gaussian.yaml`
- `../simplex_rcpo_gaussian.yaml`
- `../simplex_ppo_dirichlet.yaml`
- `../simplex_rcpo_dirichlet.yaml`
- `../rcpo_allocation_penalty.yaml`
- `../rcpo_allocation_drawdown_penalty.yaml`

This folder is a manifest only. Keeping the original files in place preserves
existing commands and checkpoint/config compatibility.
